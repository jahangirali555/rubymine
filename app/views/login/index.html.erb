<% if login_signed_in? %>

   <div class="row">
      <span class="pull-left"><h4>Welcome! <strong><%= current_login.company_name %></strong>
       ( 
             <% if current_login.usertype_id == 1 %>
              Super Admin

                <% else 
                if current_login.usertype_id == 2 %>
                  Admin
                <% else %>
                  User
                <% end %>

            <% end %>
      ) 
</h4></span>

  <%  if @usertype == @curr_usertype || @usertype2 == @curr_usertype %>
      <a href="<%= new_invite_path %>"> <button class="btn btn-primary pull-right" data-toggle="modal" data-target=".bs-example-modal-sm">Add</button></a>
  
    <br />


<!-- Sorry! You are redirected to wrong page  -->
<% end %>

  </div>


<div class="panel panel-default">
  <!-- Default panel contents -->
  <div class="panel-heading">Mr. <%= current_login.username %> </div>

  <!-- Table -->
  <table class="table">

      <thead>
        <tr>
          <th>Company</th>
          <th>Companyname</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
         <% @login.each do |log| %>
          <tr>
            <td><%= log.username %></td>
            <td><%= log.company_name %></td>
              <%  if @usertype == @curr_usertype %>
                  <td><%= link_to 'Show',  dashboard_show_path(:id => log.id) %></td>            
                  <td><%= link_to 'Edit', dashboard_edit_path(:id => log.id) %> </td>
                  <td><%= link_to 'Delete', dashboard_delete_path(:id => log.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
              <% end %>
             </tr>
        <% end %>
      </tbody>


    
  </table>
</div>






<% else %>

Sorry! You are redirected to wrong page 

<% end %>
