<% if login_signed_in? %>

      

<div class="row">
  <span class="pull-left"><h4>Welcome! <strong><%= current_login.company_name %></strong></h4></span>
 <a href="<%= new_invite_path %>"> <button class="btn btn-primary pull-right" data-toggle="modal" data-target=".bs-example-modal-sm">Add</button></a>
</div>
<br />




<!-- ## -->

<!-- <div class="modal fade bs-example-modal-sm" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-sm">
    <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
          <h4 class="modal-title">Invite Friends to Join!</h4>
        </div>

          <div class="modal-body">
            <form class="form-inline" role="form">
                
                  <div class="input-group center">
                    <input type="text" class="form-control">
                    <span class="input-group-btn">
                      <button class="btn btn-success" type="button">Invite!</button>
                    </span>
                  </div><!-- /input-group -->
               <!--  </form>

          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
            <button type="button" class="btn btn-primary">Save changes</button>
          </div>
    </div>
  </div>
</div> --> 




<!-- ## -->









<div class="panel panel-default">
  <!-- Default panel contents -->
  <div class="panel-heading">Mr. <%= current_login.username %> <strong>(Admin)</strong></div>

  <!-- Table -->
  <table class="table">

      <thead>
        <tr>
          <th>Company</th>
          <th>Companyname</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
         <% @login.each do |log| %>
          <tr>
            <td><%= log.username %></td>
            <td><%= log.company_name %></td>
         
          </tr>
        <% end %>
      </tbody>


    
  </table>
</div>

<% else %>
<%=  get '/patients/:id', to: 'patients#show' %>
<% end %>



